import React, { FC } from "react";
import Arrow from "../../assets/arrow.svg";

// MS: To be deleted later
const Icon: FC = (props) => {
  return (
    <div>
      <Arrow />
      test
    </div>
  );
};

export default Icon;
