import defaultTheme from "@kiwicom/orbit-components/lib/defaultTheme";

import typography from "./typography";

// MS: Overrides for Orbit
const orbit = {
  ...defaultTheme.orbit,
  ...typography,
};

export default orbit;
